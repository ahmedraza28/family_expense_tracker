/// A utility class that holds constants used throughout
/// the entire app.
/// This class has no constructor and all variables are `static`.
class AppConstants {
  const AppConstants._();

  /// The name of the app.
  static const String appName = 'Family Expense Tracker';
}
